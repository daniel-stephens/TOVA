"""UI agent orchestration package.

The UI currently uses a retrieval-augmented chat path; this package provides a clean
boundary for future agent/tool orchestration without bloating `ui/web/views.py`.
"""

